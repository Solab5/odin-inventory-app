<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.name %></title>
</head>
<body>
    <%- include('../header') %>
    
    <div class="container">
        <div class="details">
            <h1><i class="fas fa-box"></i> <%= item.name %></h1>
            <p><i class="fas fa-align-left"></i> <strong>Description:</strong> <%= item.description %></p>   
            <p><i class="fas fa-tag"></i> <strong>Price:</strong> $<%= item.price %></p>
            <p><i class="fas fa-cubes"></i> <strong>Stock:</strong> <%= item.stock_quantity %></p>
            <p><i class="fas fa-ruler"></i> <strong>Unit Type:</strong> <%= item.unit_type %></p>
            <p><i class="fas fa-folder"></i> <strong>Category:</strong> 
                <a href="/categories/<%= item.category_id %>"><%= item.category_name %></a>
            </p>
            
            <div class="actions">
                <a href="/items/<%= item.id %>/edit" class="btn btn-primary">
                    <i class="fas fa-edit"></i> Edit Item
                </a>
                <form action="/items/<%= item.id %>?_method=DELETE" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash"></i> Delete Item
                    </button>
                </form>
                <a href="/items" class="btn">
                    <i class="fas fa-arrow-left"></i> Back to Items
                </a>
            </div>
        </div>
    </div>
    
    <%- include('../footer') %>
</body>
</html>