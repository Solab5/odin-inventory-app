<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <%- include('../header') %>
    
    <div class="container">
        <h1><i class="fas fa-edit"></i> <%= title %></h1>
        <form action="/categories/<%= category.id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="name"><i class="fas fa-tag"></i> Name:</label>
                <input type="text" name="name" value="<%= category.name%>" required>
            </div>

            <div class="form-group">
                <label for="description"><i class="fas fa-align-left"></i> Description:</label>
                <textarea name="description" required id="description"><%= category.description %></textarea>
            </div>

            <div class="actions">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Update Category
                </button>
                <a href="/categories/<%= category.id %>" class="btn">
                    <i class="fas fa-times"></i> Cancel
                </a>
            </div>
        </form>
    </div>

    <%- include('../footer') %>
</body>
</html>